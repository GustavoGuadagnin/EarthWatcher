import styled from '@mui/system/styled'
export const ContentWrapper = styled('div')(() => ({
  width: 'auto',
  marginTop: 12,
  padding: 12,
  border: '1px solid #EDEBEB',
  borderRadius: '5px',
  marginBottom: '40px'
}))